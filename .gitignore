# Terraform state files
terraform.tfstate
terraform.tfstate.backup
.terraform/

# Lambda zip bundles (可以用 package.sh 自动打包)
lambda-*.zip

# Lock files（可选忽略）
.terraform.lock.hcl

# Node modules（如果用了 npm/yarn）
node_modules/

# 本地调试脚本或测试输出
*.log
*.tmp
*.DS_Store

# IDE / Editor junk
.vscode/
.idea/